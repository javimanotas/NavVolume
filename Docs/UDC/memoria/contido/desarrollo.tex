\chapter{Desarrollo}
\label{chap:desarrollo}

En este capítulo se describe el proceso de construcción del sistema de navegación volumétrica, siguiendo la metodología iterativa e incremental expuesta en la Sección~\ref{sec:metodologiadesarrollo}.
Cada sección se corresponde con una iteración del desarrollo y se organiza siguiendo las cuatro fases del ciclo de vida de cada incremento: el \textbf{análisis} del problema abordado, el \textbf{diseño} de la solución propuesta, su \textbf{implementación} y la \textbf{validación} del resultado obtenido.

El desarrollo comienza con una iteración preparatoria para definir la arquitectura modular y el entorno de trabajo (Iteración~0).
Sobre esta base se construye un producto mínimo viable, que incluye la representación del espacio navegable y una primera versión funcional de la búsqueda de rutas (Iteraciones~1 a~3).

A partir de ahí, el sistema se amplía de forma progresiva siguiendo la planificación descrita en la Sección~\ref{sec:planificacioninicial}.
El capítulo concluye con una discusión global que resume la arquitectura final.

\section{Iteración 0: Cimientos del proyecto}

\subsection{Análisis}

La primera iteración no incluye funcionalidad de navegación.
Su propósito es establecer una base técnica sólida sobre la que edificar el resto del sistema.
Del análisis inicial surgen dos necesidades. La primera es una arquitectura modular que favorezca el desacoplamiento y mantenga el código de edición y de pruebas fuera del producto final. La segunda es un entorno de trabajo que valide de forma automática cada contribución desde el primer momento.

\subsection{Diseño}

El sistema se organiza en módulos con responsabilidades delimitadas, separados físicamente mediante \textit{assemblies} (las unidades de compilación de C\#).
Al aislar el código de edición y de pruebas en \textit{assemblies} independientes, estos quedan excluidos de las compilaciones finales del producto, garantizando que no penalicen el tamaño ni el rendimiento del ejecutable.
La estructura resultante se divide en tres bloques, ilustrados en la Figura~\ref{fig:arquitectura}:

\begin{itemize}
    \item \textbf{Runtime:} Contiene la lógica de navegación (representación del espacio, construcción y búsqueda de rutas).
    Es el único módulo incluido en la versión final.
    \item \textbf{Editor:} Incluye las herramientas de configuración y visualización del editor de Unity.
    Depende de \textit{Runtime}, pero no a la inversa.
    \item \textbf{Tests:} Agrupa las pruebas automatizadas, divididas en modo de edición (\textit{EditMode}) y de ejecución (\textit{PlayMode}).
\end{itemize}

Internamente, el módulo \textit{Runtime} se organiza en los siguientes espacios de nombres: \texttt{Core} (estructuras de datos fundamentales), \texttt{Builder} (construcción del volumen), \texttt{Pathfinding} (búsqueda y postprocesado) y \texttt{Unity} (componentes de conexión con el motor).
A medida que el sistema creció, esta estructura se amplió con nuevos módulos como \texttt{Avoidance} (evasión local), añadido en la última iteración.
La arquitectura definitiva se detalla en la discusión final (Figura~\ref{fig:arquitectura-final}).

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[
      font=\small,
      modulo/.style={rounded corners=3pt, draw, thick, align=center,
                     inner sep=8pt, minimum width=3.4cm, minimum height=1.5cm},
      contenedor/.style={rounded corners=4pt, draw=udcpink, thick, fill=udcpink!10,
                         inner sep=10pt},
      ns/.style={rounded corners=2pt, draw=udcpink!60!black, fill=white, align=center,
                 minimum width=2.1cm, minimum height=9mm, font=\footnotesize\ttfamily},
      dep/.style={dashed, -{Stealth[length=3mm]}, thick, draw=black!70}
    ]

    % Espacios de nombres internos del modulo Runtime
    \node[ns] (core) {Core};
    \node[ns, right=3mm of core]    (builder) {Builder};
    \node[ns, right=3mm of builder] (path)    {Pathfinding};
    \node[ns, right=3mm of path]    (unity)   {Unity};

    % Contenedor Runtime (en capa de fondo para no tapar los nodos)
    \begin{scope}[on background layer]
      \node[contenedor, fit=(core)(builder)(path)(unity),
            label={[font=\bfseries]above:Runtime}] (runtime) {};
    \end{scope}

    % Modulos Editor y Tests
    \node[modulo, fill=ficblue!15, draw=ficblue,
          above=16mm of runtime.north west, anchor=south west]
      (editor) {\textbf{Editor}\\[2pt]\footnotesize Herramientas de\\configuración y\\visualización};
    \node[modulo, fill=udcgray!40, draw=udcgray!80!black,
          above=16mm of runtime.north east, anchor=south east]
      (tests) {\textbf{Tests}\\[2pt]\footnotesize Pruebas \textit{EditMode}\\y \textit{PlayMode}};

    % Dependencias unidireccionales hacia Runtime
    \draw[dep] (editor.south) -- (editor.south |- runtime.north);
    \draw[dep] (tests.south)  -- (tests.south  |- runtime.north);

  \end{tikzpicture}
  \caption[Arquitectura modular del sistema y sus dependencias.]
  {Arquitectura modular del sistema y sus dependencias.
  Los módulos \textit{Editor} y \textit{Tests} dependen del módulo \textit{Runtime} (flechas discontinuas), pero nunca a la inversa.
  Solo \textit{Runtime} se incluye en la versión final del producto.}
  \label{fig:arquitectura}
\end{figure}

\subsection{Implementación}

Cada bloque se materializa como un \textit{assembly} propio mediante los archivos de definición (\textit{assembly definitions}) de Unity.
Estos archivos declaran de forma explícita las dependencias permitidas entre módulos y obligan a que \textit{Editor} y \textit{Tests} dependan de \textit{Runtime}, pero nunca a la inversa.
Sobre esta estructura se incorpora un flujo de \acrshort{ci}: cada vez que se realiza una contribución al repositorio, se ejecutan automáticamente la herramienta de comprobación de formato Csharpier y toda la batería de pruebas.

\subsection{Validación}

Al no existir todavía funcionalidad de navegación, la validación de esta iteración consiste en comprobar que el esqueleto del proyecto es sólido: que los \textit{assemblies} compilan respetando la dirección de las dependencias y que el flujo de \acrshort{ci} funciona como una barrera de calidad.
Cuando la comprobación de formato o alguna prueba falla, los cambios quedan bloqueados y deben revisarse antes de integrarse en la rama principal.
Esta infraestructura de validación automática, establecida desde el inicio, respalda la verificación de todos los incrementos descritos en las iteraciones siguientes.

\section{Iteración 1: Representación del espacio navegable}
\label{sec:iter-representacion}

\subsection{Análisis}

El objetivo de esta iteración es implementar una estructura de datos para representar el espacio navegable de forma compacta.
El requisito principal es la eficiencia en memoria: la representación debe abarcar grandes volúmenes tridimensionales sin un coste prohibitivo y mantener los datos contiguos para favorecer el acceso a la caché.
Como se justificó en la Sección~\ref{sec:representacionespacionavegable}, se utiliza el \acrshort{svo}, una variante del \gls{octree} que únicamente subdivide las regiones con geometría.

\subsection{Diseño}

El \acrshort{svo} se estructura en capas (\textit{layers}).
Cada capa es un \textbf{vector plano} de nodos, ordenado según su código de Morton.
Las capas superiores agrupan grandes volúmenes de espacio, mientras que las inferiores aumentan progresivamente el nivel de detalle.
La capa base está formada por las \textbf{hojas}.
A diferencia de los nodos internos, las hojas no tienen ocho hijos, sino que contienen una rejilla densa de $4\times4\times4$ \glspl{voxel} (Figura~\ref{fig:hoja-mascara}).

Siguiendo los principios de diseño orientado a datos (\acrshort{dod}) de la Sección~\ref{sec:dots}, esta decisión reduce mucho la cantidad de nodos y mantiene los datos contiguos en memoria.
Para encontrar un nodo en una capa, se aplica una \textbf{búsqueda binaria} rápida, aprovechando que los códigos de Morton mantienen los elementos ordenados espacialmente.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Una capa 4x4 de la hoja; voxeles ocupados en rosa
    \foreach \c/\r in {1/0, 3/0, 0/2, 2/2, 2/3}
      \fill[ocupado] (\c*0.72,-\r*0.72) rectangle ++(0.72,-0.72);
    \foreach \c in {0,1,2,3}
      \foreach \r in {0,1,2,3}
        \draw[celda] (\c*0.72,-\r*0.72) rectangle ++(0.72,-0.72);
    \node[font=\footnotesize, anchor=south] at (1.44,0.12) {Hoja (una capa $4\times4$)};

    % Flecha de codificacion
    \draw[flecha] (3.2,-1.44) -- ++(1.45,0) node[midway, above, font=\scriptsize] {1 bit/vóxel};

    % Mascara de 64 bits (8x8)
    \begin{scope}[xshift=5cm, yshift=0.35cm]
      \foreach \c/\r in {1/0,3/0,5/1,0/2,2/2,6/3,2/3,4/4,7/5,1/6,3/6,5/7}
        \fill[ocupado] (\c*0.34,-\r*0.34) rectangle ++(0.34,-0.34);
      \foreach \c in {0,...,7}
        \foreach \r in {0,...,7}
          \draw[celda] (\c*0.34,-\r*0.34) rectangle ++(0.34,-0.34);
      \node[font=\footnotesize, anchor=south] at (1.36,0.12) {Máscara de 64 bits (\texttt{ulong})};
    \end{scope}
  \end{tikzpicture}
  \caption[Hoja del \acrshort{svo} y su máscara de ocupación de 64 bits.]
  {Hoja del \acrshort{svo} y su máscara de ocupación de 64 bits.
  Cada hoja apila cuatro capas de $4\times4$ vóxeles. Cada uno de esos $4\times4\times4=64$ vóxeles se corresponde con un bit de un único entero de 64 bits (\texttt{ulong}): los ocupados (en rosa) fijan su bit a~1 y los libres a~0.}
  \label{fig:hoja-mascara}
\end{figure}

\subsection{Implementación}

\subsubsection{Codificación de las hojas mediante manipulación de bits}

Cada hoja registra su estado utilizando un \textbf{único entero de 64 bits} (\texttt{ulong}), donde cada bit indica si un \gls{voxel} está libre u ocupado.
Gracias a esta manipulación de bits, una hoja completa ocupa apenas 8 bytes.
Además, permite comprobar al instante si la hoja entera está vacía o llena con una única comparación numérica, sin inspeccionar los 64 \glspl{voxel} uno a uno.

Para traducir las coordenadas tridimensionales de un \gls{voxel} a su posición en los bits, se realizan desplazamientos que comprimen las tres coordenadas en un índice de 6 bits.

\subsubsection{Códigos de Morton}

Para mapear las coordenadas 3D $(x, y, z)$ en un índice lineal que mantenga cercanos los nodos contiguos, se utilizan los códigos de Morton (basados en las curvas de orden Z de la Sección~\ref{sec:representacionespacionavegable}).
Este código se calcula intercalando los bits de las tres coordenadas mediante desplazamientos y máscaras (Figura~\ref{fig:morton-interleave}).

\begin{figure}[H]
\begin{lstlisting}[language={[Sharp]C}]
// Intercala dos ceros entre cada bit del valor de entrada.
static uint Interleave00(uint x)
{
    x = (x ^ (x << 16)) & 0xFF0000FF;
    x = (x ^ (x <<  8)) & 0x0300F00F;
    x = (x ^ (x <<  4)) & 0x030C30C3;
    x = (x ^ (x <<  2)) & 0x09249249;
    return x;
}

// El codigo final entrelaza los bits de las tres coordenadas.
public MortonCode(int x, int y, int z) =>
    _code = Interleave00((uint)x)
          | (Interleave00((uint)y) << 1)
          | (Interleave00((uint)z) << 2);
\end{lstlisting}
\caption{Codificación de un código de Morton mediante intercalado de bits.}
\label{fig:morton-interleave}
\end{figure}

Este enfoque simplifica las operaciones jerárquicas.
Para obtener el código de un nodo padre basta con desplazar sus coordenadas un bit a la derecha.
Para los hijos, se desplazan a la izquierda añadiendo el bit del octante correspondiente.
Al usar enteros de 32 bits, la resolución máxima del sistema queda limitada a $1024$ ($2^{10}$) celdas por eje.

\subsubsection{Punteros compactos}
\label{sec:svolink}

Por último, se implementó un mecanismo de referencia entre nodos más eficiente.
En lugar de usar punteros estándar de C\#, se diseñó un \textbf{puntero compacto} de 32 bits.
Este único entero contiene toda la información necesaria para localizar cualquier nodo o \gls{voxel} (Figura~\ref{fig:svolink-bits}).

\begin{figure}[H]
\begin{lstlisting}[language={[Sharp]C}]
const uint _IS_NODE_MASK       = 1u << 31;       // bit 31:    tipo (1 nodo, 0 voxel)
const uint _OFFSET_MASK        = 0x1FFFFFF << 6; // bits 30..6: offset (25 bits)
const uint _CONCRETE_DATA_MASK = 0x3F;           // bits  5..0: capa o subnodo (6 bits)
\end{lstlisting}
\caption{Disposición de los 32 bits del enlace (\texttt{SVOLink}).}
\label{fig:svolink-bits}
\end{figure}

El bit más significativo indica si el puntero señala a un nodo o a un \gls{voxel}, los 25 bits centrales guardan la posición (desplazamiento) dentro de la capa y los 6 bits finales indican el nivel de la capa o el índice específico dentro de la hoja.
Este diseño compacto ahorra memoria y permite usar el puntero directamente como clave en las tablas \textit{hash} de la búsqueda de rutas.
Para maximizar su rendimiento, la estructura implementa la interfaz \texttt{IEquatable}, evitando problemas de \textit{boxing} en secciones críticas del algoritmo.

La Figura~\ref{fig:core-clases} muestra un resumen visual de las estructuras de datos del módulo \texttt{Core} y sus relaciones principales.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[
      font=\small,
      clase/.style={draw=ficblue!75!black, fill=ficblue!7, thick, rounded corners=1pt,
                    rectangle split, rectangle split parts=3, align=left,
                    text width=3.3cm, inner xsep=5pt, inner ysep=3.5pt, font=\scriptsize},
      comp/.style={draw=ficblue!75!black, thick,
                   -{Diamond[length=2.8mm, width=1.9mm, fill=ficblue!75!black]}},
      deref/.style={draw=black!65, dashed, -{Stealth[length=2.2mm]}, thick},
      etiq/.style={font=\scriptsize, fill=white, inner sep=1pt},
      mult/.style={font=\scriptsize, inner sep=1.5pt}
    ]

    % --- Clases ---
    \node[clase] (svo) at (0,0) {
      {\itshape class}\\[-1pt]\textbf{SVO}
      \nodepart{two}\ttfamily Layers : SVONode[][]\\ LeafNodes : SVOLeaf[]
      \nodepart{three}\ttfamily GetNode(SVOLink)\\ TryFindNodeIndex()\\ IsVoxelOccupied()
    };

    \node[clase] (node) at (0,-3.7) {
      {\itshape struct}\\[-1pt]\textbf{SVONode}
      \nodepart{two}\ttfamily MortonCode\\ FirstChild : SVOLink\\ Parent : SVOLink\\ Neighbors : NeighborSet
      \nodepart{three}\ttfamily HasChildren : bool
    };

    \node[clase] (leaf) at (4.7,-3.7) {
      {\itshape struct}\\[-1pt]\textbf{SVOLeaf}
      \nodepart{two}\ttfamily occupiedSubnodes : ulong
      \nodepart{three}\ttfamily IsOccupied(i)\\ IsEmpty / IsFull
    };

    \node[clase] (morton) at (-4.4,-7.6) {
      {\itshape struct}\\[-1pt]\textbf{MortonCode}
      \nodepart{two}\ttfamily code : uint
      \nodepart{three}\ttfamily ParentCode\\ ChildCode(i)\\ TryGetNeighborCode()
    };

    \node[clase] (nset) at (0,-7.6) {
      {\itshape struct}\\[-1pt]\textbf{NeighborSet}
      \nodepart{two}\ttfamily posX, negX, posY,\\ negY, posZ, negZ : SVOLink
      \nodepart{three}\ttfamily this[Direction]
    };

    \node[clase] (link) at (4.4,-7.6) {
      {\itshape struct}\\[-1pt]\textbf{SVOLink}
      \nodepart{two}\ttfamily link : uint
      \nodepart{three}\ttfamily IsNode(out layer)\\ IsVoxel(out sub)\\ Offset
    };

    % --- Relaciones (rombo en el contenedor, flecha discontinua para referencias) ---
    \draw[comp] (node.north) -- node[etiq, midway] {Layers} (svo.south);
    \draw[comp] (leaf.north) -- node[etiq, pos=0.62] {LeafNodes} (svo.east);
    \draw[comp] (morton.north) -- node[mult, pos=0.85, left] {1} (node.south west);
    \draw[comp] (nset.north) -- node[etiq, midway] {Neighbors} (node.south);
    \draw[comp] (link.west) -- node[mult, midway, above] {6} (nset.east);
    \draw[deref] (node.south east) -- node[etiq, pos=0.55] {FirstChild / Parent} (link.north);

  \end{tikzpicture}
  \caption[Estructuras de datos del espacio de nombres \texttt{Core} y sus relaciones.]
  {Estructuras de datos del espacio de nombres \texttt{Core} y sus relaciones.
  Los rombos denotan composición (propiedad de un valor) y las flechas discontinuas, referencias mediante enlaces.
  El \acrshort{svo} agrega las capas de \texttt{SVONode} y el vector paralelo de \texttt{SVOLeaf}. Cada nodo referencia a su primer hijo y a su padre, y agrupa su conjunto de seis vecinos, todos ellos codificados como \texttt{SVOLink} (el puntero compacto de la Sección~\ref{sec:svolink}).}
  \label{fig:core-clases}
\end{figure}

\subsection{Validación}

Dado que estas estructuras manipulan los datos a nivel de bits, su corrección es crítica y se verificó mediante pruebas unitarias automatizadas.
Para cubrir el amplio rango de valores posibles se recurrió a \textbf{pruebas basadas en propiedades} (\textit{property-based testing}): en lugar de comprobar casos concretos, se generan entradas aleatorias y se verifica que se cumpla siempre una propiedad invariante.
El ejemplo más representativo es la codificación de Morton, donde para cualquier coordenada $(x, y, z)$ generada al azar dentro del rango válido, decodificar su código de Morton debe devolver exactamente la coordenada original (propiedad de ida y vuelta).
La misma estrategia se aplicó a la conversión entre coordenadas y máscara de bits de las hojas y a la codificación y decodificación de los punteros compactos (\texttt{SVOLink}), garantizando que la información empaquetada en cada entero se recupera sin pérdidas.

\section{Iteración 2: Construcción y rasterización del volumen}
\label{sec:iter-construccion}

\subsection{Análisis}

Una vez definida la estructura de datos, esta iteración se centra en su construcción a partir de la geometría de la escena.
Este proceso, denominado \textbf{rasterización}, consiste en identificar qué regiones del espacio están ocupadas por obstáculos para rellenar el \acrshort{svo}.
Al ser una de las fases con mayor coste computacional del sistema, el principal requisito de esta iteración es el rendimiento, por lo que su diseño se orienta completamente hacia la optimización.

\subsection{Diseño}

La construcción se organiza como un flujo de trabajo (\textit{pipeline}) dividido en etapas independientes, el cual se resume en la Figura~\ref{fig:build-pipeline}.
Cada etapa consume los datos generados por la anterior, y aquellas que requieren un gran número de consultas físicas se delegan en el \textit{Job System} para su ejecución en paralelo.
Sobre esta canalización se aplican dos optimizaciones transversales que guían todo el diseño: una estrategia jerárquica \textit{de lo grueso a lo fino}, que descarta cuanto antes las regiones vacías, y el descarte temprano de las hojas sin geometría.
A continuación se detallan las etapas y las técnicas que las implementan.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[
      font=\small, node distance=5mm,
      etapa/.style={paso, text width=3.9cm, minimum height=1.0cm},
      tienda/.style={almacen, text width=3.9cm, minimum height=1.0cm},
      dato/.style={draw=udcgray!70!black, fill=udcgray!12, rounded corners=2pt,
                   align=left, font=\scriptsize, text width=3.0cm, inner sep=4pt},
      job/.style={draw=ficblue!60, fill=ficblue!8, rounded corners=2pt,
                  align=center, font=\scriptsize, text width=2.6cm, inner sep=4pt},
      conector/.style={densely dotted, draw=udcgray!75},
      conjob/.style={densely dotted, draw=ficblue!65}
    ]

    % --- Columna central: etapas de la canalizacion ---
    \node[tienda] (escena) {Escena\\[1pt]{\scriptsize(\textit{colliders})}};
    \node[etapa, below=of escena] (s1) {\textbf{1.~Rasterización L1}\\[1pt]{\scriptsize barrido grueso-a-fino}};
    \node[etapa, below=of s1] (s2) {\textbf{2.~Reserva de la capa 0}\\[1pt]{\scriptsize creación de nodos hoja}};
    \node[etapa, below=of s2] (s3) {\textbf{3.~Rasterización de hojas}\\[1pt]{\scriptsize descarte grueso + muestreo fino}};
    \node[etapa, below=of s3] (s4) {\textbf{4.~Capas superiores}\\[1pt]{\scriptsize de la raíz hacia las hojas}};
    \node[etapa, below=of s4] (s5) {\textbf{5.~Enlazado padre--hijos}};
    \node[etapa, below=of s5] (s6) {\textbf{6.~Enlazado de vecinos}};
    \node[tienda, below=of s6] (out) {SVO enlazado\\[1pt]{\scriptsize(\textit{NavContext})}};

    \foreach \a/\b in {escena/s1, s1/s2, s2/s3, s3/s4, s4/s5, s5/s6, s6/out}
      \draw[flecha] (\a) -- (\b);

    % --- Columna derecha: productos de datos ---
    \node[dato, right=9mm of s1] (d1) {códigos Morton de las celdas L1 ocupadas};
    \node[dato, right=9mm of s2] (d2) {\texttt{SVONode[]} de la capa 0};
    \node[dato, right=9mm of s3] (d3) {máscaras de 64\,bits (\texttt{SVOLeaf[]})};
    \node[dato, right=9mm of s4] (d4) {nodos internos por capa};
    \node[dato, right=9mm of s5] (d5) {enlaces \texttt{FirstChild} / \texttt{Parent}};
    \node[dato, right=9mm of s6] (d6) {\texttt{NeighborSet} (6 vecinos/nodo)};
    \foreach \s/\d in {s1/d1, s2/d2, s3/d3, s4/d4, s5/d5, s6/d6}
      \draw[conector] (\s.east) -- (\d.west);

    % --- Columna izquierda: etapas paralelizadas ---
    \node[job, left=9mm of s1] (j1) {\textit{Job System} + Burst\\[1pt]\texttt{OverlapBox} por lotes};
    \node[job, left=9mm of s3] (j3) {\textit{Job System} + Burst\\[1pt]descarte / fino / reducción};
    \draw[conjob] (j1.east) -- (s1.west);
    \draw[conjob] (j3.east) -- (s3.west);

  \end{tikzpicture}
  \caption[Canalización de construcción del \acrshort{svo}.]
  {Canalización de construcción del \acrshort{svo} durante el \gls{bake}.
  Cada etapa (centro) consume el producto de datos de la anterior (derecha).
  Las etapas~1 y~3, dominadas por consultas físicas, se aceleran mediante el \textit{Job System} y Burst (izquierda) y se detallan en las Figuras~\ref{fig:coarse-to-fine} y~\ref{fig:empty-leaf}, mientras que el enlazado de la vecindad (etapa~6) se ilustra en la Figura~\ref{fig:neighbor-link}.}
  \label{fig:build-pipeline}
\end{figure}

\subsection{Implementación}

\subsubsection{Muestreo del espacio mediante consultas físicas paralelizadas}

Como se mencionó en los fundamentos, el sistema no analiza directamente la geometría de las mallas.
En su lugar, delega la detección de obstáculos en el motor de físicas mediante consultas \texttt{OverlapBox}, las cuales verifican si una caja determinada colisiona con algún \textit{collider}.
Debido al elevado volumen de consultas necesarias, estas se agrupan en lotes (\textit{batches}) a través de la estructura \texttt{OverlapBoxCommand} y se ejecutan en paralelo en hilos secundarios gracias al \textit{Job System} y al compilador Burst (Sección~\ref{sec:dots}).

Para optimizar este proceso, se emplea una estrategia \textbf{de lo grueso a lo fino} (\textit{coarse-to-fine}).
En lugar de analizar todo el espacio a la máxima resolución desde el principio, el barrido comienza en una capa cercana a la raíz.
En cada nivel, solo se subdividen aquellas celdas que contienen geometría, lo que permite descartar por completo los subárboles vacíos (Figura~\ref{fig:coarse-to-fine}).
Este descarte temprano es la primera gran optimización de la fase de construcción.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Izquierda: nivel grueso
    \begin{scope}
      \fill[obstaculo, rounded corners=2pt] (1.0,-1.0) rectangle (2.7,-2.7);
      \foreach \c in {0,1,2,3}\foreach \r in {0,1,2,3}
        \draw[celda, line width=0.7pt] (\c*0.8,-\r*0.8) rectangle ++(0.8,-0.8);
      \foreach \c/\r in {1/1,2/1,1/2,2/2}
        \draw[draw=udcpink, very thick] (\c*0.8,-\r*0.8) rectangle ++(0.8,-0.8);
      \node[font=\footnotesize, anchor=north, text width=3.4cm, align=center] at (1.6,-3.5)
        {Nivel grueso: se detectan las celdas con geometría};
    \end{scope}
    % Flecha
    \draw[flecha] (3.5,-1.6) -- ++(1.3,0) node[midway, above, font=\scriptsize] {subdivisión};
    % Derecha: nivel fino
    \begin{scope}[xshift=5.3cm]
      \fill[obstaculo, rounded corners=2pt] (1.0,-1.0) rectangle (2.7,-2.7);
      \foreach \c in {0,1,2,3}\foreach \r in {0,1,2,3}
        \draw[celda, line width=0.7pt] (\c*0.8,-\r*0.8) rectangle ++(0.8,-0.8);
      \foreach \c/\r in {1/1,2/1,1/2,2/2}
        \foreach \sc in {0,1}\foreach \sr in {0,1}
          \draw[celda] (\c*0.8+\sc*0.4,-\r*0.8-\sr*0.4) rectangle ++(0.4,-0.4);
      \foreach \c/\r in {1/1,2/1,1/2,2/2}
        \draw[draw=udcpink, very thick] (\c*0.8,-\r*0.8) rectangle ++(0.8,-0.8);
      \node[font=\footnotesize, anchor=north, text width=3.6cm, align=center] at (1.6,-3.5)
        {Nivel fino: solo esas celdas se subdividen};
    \end{scope}
  \end{tikzpicture}
  \caption[Barrido jerárquico de lo grueso a lo fino durante la rasterización.]
  {Barrido jerárquico de lo grueso a lo fino durante la rasterización.
  Las celdas vacías se descartan por completo. Solo las que contienen geometría (resaltadas en rosa) se subdividen en el siguiente nivel de resolución.}
  \label{fig:coarse-to-fine}
\end{figure}

\subsubsection{Optimización: descarte de hojas vacías}
\label{sec:descarte-hojas}

La segunda optimización, y una de las más importantes para el rendimiento, se aplica durante la rasterización de los nodos hoja.
Comprobar la ocupación de una hoja completa requeriría, en principio, realizar 64 consultas \texttt{OverlapBox} (una por cada \gls{voxel}).
Sin embargo, en la mayoría de los casos este coste se puede evitar.

Esto se debe a una característica del diseño de niveles: en un entorno tridimensional típico, el espacio transitable es \textbf{mayoritariamente aire libre}.
Los obstáculos (como paredes, edificios o el terreno) ocupan una fracción pequeña del volumen total, por lo que la mayor parte de las hojas están vacías.
Esta propiedad es la que justifica el uso de una representación dispersa.

Para aprovecharla, la rasterización de las hojas se divide en dos pasadas consecutivas (ilustradas en las Figuras~\ref{fig:empty-leaf} y~\ref{fig:empty-leaf-pseudo}):

\begin{enumerate}
    \item \textbf{Pasada gruesa de descarte:} Se realiza una \textbf{única} consulta \texttt{OverlapBox} que abarca la totalidad de la hoja.
    Esta caja coincide con el volumen envolvente de sus 64 vóxeles (expandido según el radio del agente).
    Si esta consulta no detecta ningún obstáculo, la hoja se marca directamente como vacía y se descarta el resto del análisis.
    \item \textbf{Pasada fina:} Solo aquellas hojas que han superado el filtro anterior (donde sí se detectó geometría) realizan las 64 consultas individuales para determinar con precisión qué vóxeles concretos están ocupados.
\end{enumerate}

Dado que las hojas con obstáculos son una minoría, esta optimización reduce el número de consultas físicas en un factor cercano a 64 en la mayor parte del volumen.
Finalmente, un \textit{job} de reducción condensa el resultado de la pasada fina en la máscara de 64 bits de cada hoja.

\begin{figure}[H]
\begin{lstlisting}
// --- Pasada gruesa: una consulta por hoja completa ---
lanzar  BuildCoarseLeafCommands   -> comandos_gruesos   (1 por hoja)
ejecutar OverlapBox (en lote)     -> resultados_gruesos
supervivientes = { i : resultados_gruesos[i] golpea algo }

si no hay supervivientes:
    todas las hojas quedan vacias  // caso muy comun

// --- Pasada fina: solo sobre las supervivientes ---
lanzar  BuildLeafCommands(supervivientes) -> comandos  (64 por superviviente)
ejecutar OverlapBox (en lote)             -> resultados
lanzar  ReduceLeafBitmasks                -> mascara[superviviente]

hojas[*] = Vacia
para cada j en supervivientes:
    hojas[j] = DesdeBits(mascara[j])
\end{lstlisting}
\caption{Rasterización de hojas en dos pasadas: descarte grueso y muestreo fino.}
\label{fig:empty-leaf-pseudo}
\end{figure}

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Izquierda: pasada gruesa, 1 caja envolvente
    \begin{scope}
      \foreach \c in {0,1,2,3}\foreach \r in {0,1,2,3}
        \draw[celda] (\c*0.7,-\r*0.7) rectangle ++(0.7,-0.7);
      \draw[draw=ficblue, very thick, rounded corners=2pt] (-0.12,0.12) rectangle (2.92,-2.92);
      \node[font=\footnotesize, anchor=north, text width=3.4cm, align=center] at (1.4,-3.2)
        {Pasada gruesa:\\ \textbf{1} consulta envolvente};
    \end{scope}
    \draw[flecha] (3.4,-1.4) -- ++(1.2,0);
    % Derecha: pasada fina, 1 caja por voxel
    \begin{scope}[xshift=5cm]
      \foreach \c in {0,1,2,3}\foreach \r in {0,1,2,3} {
        \draw[celda] (\c*0.7,-\r*0.7) rectangle ++(0.7,-0.7);
        \draw[draw=ficblue, thick] (\c*0.7+0.07,-\r*0.7-0.07) rectangle ++(0.56,-0.56);
      }
      \node[font=\footnotesize, anchor=north, text width=3.7cm, align=center] at (1.4,-3.2)
        {Pasada fina:\\ \textbf{64} consultas individuales};
    \end{scope}
  \end{tikzpicture}
  \caption[Pasada gruesa de descarte frente a pasada fina de muestreo.]
  {Pasada gruesa de descarte frente a pasada fina de muestreo.
  La pasada gruesa resuelve la hoja entera con una única consulta envolvente. Solo si esta detecta geometría se ejecuta la pasada fina, con una consulta por vóxel (64 en una hoja de $4\times4\times4$).}
  \label{fig:empty-leaf}
\end{figure}

\subsubsection{Enlazado de la vecindad}
\label{sec:enlazado-vecinos}

Tras rasterizar las hojas y generar las capas superiores, la última etapa consiste en conectar cada nodo con sus seis vecinos adyacentes (en las direcciones $\pm X$, $\pm Y$ y $\pm Z$).
Esta información es indispensable para que el algoritmo de búsqueda de rutas pueda desplazarse a través del grafo.

Este enlazado se resuelve recorriendo las capas desde la raíz hacia las hojas mediante un sistema de \textbf{herencia del nodo padre}.
Para cada nodo y dirección, se comprueba primero si existe un vecino del mismo tamaño utilizando su código de Morton.
Si no se encuentra, en lugar de realizar una búsqueda exhaustiva entre nodos de distintos tamaños, el nodo simplemente hereda el enlace del vecino de su padre (un nodo de mayor dimensión).
Esta estrategia, ilustrada en la Figura~\ref{fig:neighbor-link}, resuelve la transición entre regiones con distinto nivel de resolución, uno de los problemas más difíciles de la navegación sobre estructuras jerárquicas.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Vecino grande (nivel del padre)
    \draw[celda, very thick, fill=ficblue!12] (0,0) rectangle (2.6,-2.6);
    \node[font=\footnotesize, align=center] at (1.3,-1.85) {Vecino de\\mayor tamaño};
    % Nodo a enlazar, subdividido
    \begin{scope}[xshift=2.6cm]
      \draw[celda, very thick] (0,0) rectangle (2.6,-2.6);
      \draw[celda] (1.3,0) -- (1.3,-2.6);
      \draw[celda] (0,-1.3) -- (2.6,-1.3);
      \foreach \sc in {0,1}\foreach \sr in {0,1}
        \draw[celda] (\sc*0.65,-\sr*0.65) rectangle ++(0.65,-0.65);
      \fill[udcpink!65] (0,0) rectangle (0.65,-0.65);
      \draw[celda] (0,0) rectangle (0.65,-0.65);
    \end{scope}
    \node[font=\scriptsize, anchor=west] at (3.4,-0.32) {nodo pequeño};
    % Enlace heredado del padre
    \draw[flecha, draw=udcpink, very thick] (2.6,-0.32) to[out=180, in=0] (1.6,-0.78);
    \node[font=\scriptsize, text=udcpink!80!black, anchor=north] at (1.5,-1.0) {vecino heredado};
  \end{tikzpicture}
  \caption[Enlazado de la vecindad mediante herencia del nodo padre.]
  {Enlazado de la vecindad mediante herencia del nodo padre.
  Cuando un nodo pequeño carece de un vecino de su mismo tamaño en una dirección, hereda como vecino el nodo de mayor tamaño adyacente a su padre, evitando una búsqueda exhaustiva entre celdas de distinto tamaño.}
  \label{fig:neighbor-link}
\end{figure}

Al concluir esta etapa, el sistema ya es capaz de transformar cualquier escena tridimensional en un \acrshort{svo} completamente conectado y listo para la navegación.

\subsection{Validación}

La construcción se validó comprobando que el volumen resultante refleja fielmente la geometría de la escena.
Para ello se emplearon pruebas en modo de ejecución (\textit{PlayMode}), que levantan escenas con obstáculos conocidos, ejecutan la rasterización y verifican que los \glspl{voxel} marcados como ocupados se corresponden con la geometría real consultada al motor de físicas.
La optimización de descarte de hojas vacías recibió especial atención: se contrastó que el resultado de la pasada en dos fases coincide con el de un muestreo exhaustivo \gls{voxel} a \gls{voxel}, asegurando que la aceleración no introduce falsos negativos.
Finalmente, el enlazado de la vecindad se verificó mediante pruebas de integración que recorren el árbol y comprueban la \textbf{simetría} de los vínculos (si un nodo apunta a un vecino, ese vecino debe reconocerlo en la dirección opuesta), un invariante imprescindible para que la búsqueda de rutas sea correcta.

\section{Iteración 3: Búsqueda de rutas tridimensional}
\label{sec:iter-pathfinding}

\subsection{Análisis}

Esta iteración desarrolla el núcleo del sistema y completa el producto mínimo viable: calcular una trayectoria libre de colisiones entre dos puntos.
Para lograrlo, se adapta el algoritmo \gls{astar} para que opere sobre el \acrshort{svo}, tal como se planteó en el capítulo de fundamentos.
El principal reto es que, a diferencia de una implementación clásica de \gls{astar} sobre una cuadrícula uniforme, este buscador opera sobre un grafo de \textbf{resolución heterogénea}: un único salto puede cruzar desde un nodo gigante de espacio abierto hasta un pequeño \gls{voxel} pegado a un obstáculo.

\subsection{Diseño}

\subsubsection{Búsqueda sobre un grafo de resolución heterogénea}

Para gestionar esta diferencia de tamaños, el algoritmo adapta su comportamiento al expandir los nodos vecinos (Figura~\ref{fig:astar-multinivel}):

\begin{itemize}
    \item \textbf{Nodo de espacio libre:} Se cruza en un solo salto.
    Esto permite atravesar grandes áreas vacías explorando muy pocos nodos.
    \item \textbf{Nodo con geometría:} Al contener obstáculos, no se puede cruzar de forma segura.
    El algoritmo lo subdivide y evalúa sus ocho hijos a mayor resolución.
    \item \textbf{Hoja parcialmente ocupada:} Se examinan directamente sus \glspl{voxel} libres, ignorando los que están bloqueados por la geometría.
\end{itemize}

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Marco exterior
    \draw[celda, line width=0.7pt] (0,0) rectangle (6.4,-3.6);
    % Zona abierta (izquierda): pocas celdas grandes
    \draw[celda, line width=0.7pt] (1.8,0) -- (1.8,-3.6);
    \draw[celda, line width=0.7pt] (3.6,0) -- (3.6,-3.6);
    \draw[celda, line width=0.7pt] (0,-1.8) -- (3.6,-1.8);
    % Zona fina (derecha, cerca del obstaculo)
    \foreach \c in {0,...,3}\foreach \r in {0,...,7}
      \draw[celda] (3.6+\c*0.7,-\r*0.45) rectangle ++(0.7,-0.45);
    % Obstaculo
    \fill[obstaculo, rounded corners=2pt] (4.2,-1.0) rectangle (5.6,-2.7);
    \node[font=\footnotesize, text=white] at (4.9,-1.85) {obstáculo};
    % Ruta A*
    \node[nodo, fill=ficblue!40] (s) at (0.7,-0.9) {};
    \node[font=\scriptsize, anchor=south] at (0.7,-0.62) {inicio};
    \coordinate (a) at (2.7,-0.9);
    \coordinate (b) at (3.9,-0.7);
    \coordinate (c) at (5.0,-0.55);
    \coordinate (d) at (5.95,-1.2);
    \coordinate (e) at (5.95,-2.6);
    \coordinate (f) at (5.2,-3.15);
    \node[nodo, fill=udcpink!75] (g) at (4.4,-3.15) {};
    \node[font=\scriptsize, anchor=north] at (4.4,-3.35) {meta};
    \draw[flecha, draw=udcpink, very thick] (s) -- (a) -- (b) -- (c) -- (d) -- (e) -- (f) -- (g);
    \foreach \p in {a,b,c,d,e,f} \fill[udcpink] (\p) circle (1.7pt);
    \node[font=\scriptsize, text=udcpink!80!black, anchor=south] at (1.7,-0.85) {salto grande};
    \node[font=\scriptsize, text=udcpink!80!black, anchor=west] at (6.0,-1.95) {pasos finos};
  \end{tikzpicture}
  \caption[Expansión de A* sobre un grafo de resolución heterogénea.]
  {Expansión de A* sobre un grafo de resolución heterogénea.
  En el espacio abierto la búsqueda avanza con saltos grandes entre nodos de gran tamaño. Al aproximarse al obstáculo, se ramifica en nodos y vóxeles cada vez más pequeños para bordearlo con precisión.}
  \label{fig:astar-multinivel}
\end{figure}

\begin{figure}[H]
\begin{lstlisting}
empujar(inicio, f = h(inicio))
mientras lista_abierta no vacia:
    n = extraer_minimo()
    si cerrado[n]: continuar
    marcar cerrado[n]
    si n == meta: devolver reconstruir(n)

    para cada m en vecinos(n):
        si m es nodo grande con geometria: expandir sus hijos; continuar
        si m es hoja parcialmente ocupada: expandir sus voxeles libres; continuar
        si bloqueado(m): continuar

        g_nuevo = g[n] + coste(n, m)
        si g_nuevo < g[m]:
            g[m] = g_nuevo;  procedencia[m] = n
            empujar(m, g_nuevo + h(m))
\end{lstlisting}
\caption{Esquema del bucle principal de la búsqueda \gls{astar} adaptada.}
\label{fig:astar-pseudo}
\end{figure}

\subsubsection{Función de coste y heurística}

El sistema permite calcular el coste de la trayectoria de dos maneras:

\begin{itemize}
    \item \textbf{Distancia euclídea:} Mide la distancia física real entre los centros de los nodos, generando la ruta más corta posible geométricamente.
    \item \textbf{Conteo de nodos:} Asigna un valor de coste igual a 1 para cualquier salto, sin importar el tamaño del nodo.
    Al penalizar todos los movimientos por igual, el algoritmo prefiere pasar por unos pocos nodos grandes antes que por muchos pequeños.
    Esto aleja al agente de los obstáculos y lo guía hacia espacios más abiertos.
\end{itemize}

La heurística utilizada es la distancia en línea recta hasta la meta, multiplicada por un peso configurable $W$ (búsqueda voraz).
En el modo de \textit{conteo de nodos}, dado que el coste $g$ no representa una distancia física, la heurística se divide por el tamaño del nodo más grande.
De esta forma, se garantiza que ambos valores mantengan la misma escala al aplicar la fórmula $f(n) = g(n) + W \cdot h(n)$.

\subsection{Implementación}

El buscador usa una cola de prioridad para la lista abierta.
El puntero compacto (\texttt{SVOLink}) sirve de clave en las tablas \textit{hash} que registran el coste acumulado y la procedencia de cada nodo.
Como implementa \texttt{IEquatable} (Sección~\ref{sec:svolink}), estas operaciones tan frecuentes evitan el \textit{boxing}.

Al llegar a la meta, la ruta final se reconstruye retrocediendo por los nodos a través de los enlaces de procedencia que se fueron guardando durante la búsqueda.
Este proceso también maneja casos especiales:

\begin{itemize}
  \item \textbf{Escenarios triviales:} volúmenes completamente vacíos o punto de inicio igual al punto de fin.
  \item \textbf{Rutas imposibles:} rechaza puntos de inicio o fin inválidos (ubicados dentro de un obstáculo).
  \item \textbf{Caminos muy costosos:} establece un límite máximo de nodos a explorar para evitar que el sistema se bloquee buscando rutas imposibles.
\end{itemize}

\subsection{Validación}

El buscador se validó con pruebas unitarias en modo de edición (\textit{EditMode}) que ejercitan escenarios controlados: rutas en volúmenes vacíos, coincidencia entre el punto de inicio y el de fin, y trazados que deben rodear obstáculos sencillos.
Estas pruebas comprueban tanto que la ruta encontrada es continua (cada nodo es vecino del siguiente) como que el algoritmo termina correctamente en los casos límite, devolviendo un fallo controlado cuando la meta es inalcanzable o cuando se supera el límite de nodos explorados.

Al finalizar esta iteración, se alcanza el objetivo mínimo del proyecto: el sistema ya es capaz de generar el volumen y trazar rutas navegables sobre él.
Sin embargo, garantizar que estas rutas sean fiables y se ajusten a las dimensiones físicas del agente resultó más difícil de lo previsto, como se verá en la Iteración~6 (Sección~\ref{sec:iter-robustez}).

\section{Iteración 4: Postprocesado de trayectorias}
\label{sec:iter-postprocesado}

\subsection{Análisis}

Las rutas generadas directamente sobre el volumen discretizado suelen parecer artificiales, ya que están formadas por múltiples segmentos rígidos alineados con la cuadrícula.
El objetivo de esta iteración es dotar al agente de un movimiento más orgánico sin comprometer la seguridad de la trayectoria.

\subsection{Diseño}

Para lograrlo se implementa un postprocesado dividido en dos fases complementarias, tal y como se describió en los fundamentos.
La primera es un \textbf{atajo voraz} basado en comprobaciones de línea de visión, que elimina los nodos redundantes de la ruta.
La segunda \textbf{suaviza} la trayectoria resultante mediante una \textit{spline} de Catmull-Rom.
Ambas fases se conciben para preservar la seguridad de la ruta: cada una opera únicamente sobre puntos que ya se han verificado como libres de colisión.

\subsection{Implementación}

\subsubsection{Atajo voraz mediante línea de visión}

La primera fase aplica una variante voraz del algoritmo \textit{string pulling} para eliminar los nodos redundantes de la ruta.
Empezando por el primer punto, el algoritmo intenta trazar una línea recta hasta el punto más lejano posible.
Si existe \textbf{línea de visión directa} entre ambos (es decir, ningún obstáculo bloquea el paso), se descartan todos los nodos intermedios.

Comprobar esta línea de visión es un proceso crítico tanto para la precisión como para el rendimiento del sistema.
Para abordarlo de forma eficiente, se utiliza el algoritmo \acrshort{dda}, que recorre de forma progresiva los \glspl{voxel} que atraviesa la línea recta para verificar si alguno está ocupado por geometría.
En cada paso, el recorrido avanza por el eje cuyo límite (el plano del \gls{voxel}) esté más cerca.
Esto garantiza que no se omita ningún \gls{voxel} ni se evalúe el mismo dos veces.
El procedimiento se resume en la Figura~\ref{fig:dda-pseudo}.

\begin{figure}[H]
\begin{lstlisting}
celda   = floor(inicio)              // voxel inicial
paso    = signo(direccion)           // +1 o -1 por eje
tDelta  = 1 / |direccion|            // avance parametrico por eje
tCruce  = distancia al primer plano de voxel por eje

mientras dentro de la rejilla:
    si ocupado(celda): devolver false      // obstaculo: sin linea de vision

    eje = aquel con menor tCruce            // siguiente plano mas cercano
    si tCruce[eje] >= longitud_segmento: terminar
    celda[eje]  += paso[eje]
    tCruce[eje] += tDelta[eje]

devolver true
\end{lstlisting}
\caption{Recorrido \acrshort{dda} para la comprobación de línea de visión.}
\label{fig:dda-pseudo}
\end{figure}

\subsubsection{Suavizado mediante \textit{splines} de Catmull-Rom}

Una vez eliminados los nodos innecesarios, la trayectoria final se suaviza trazando una curva \textit{spline} de Catmull-Rom entre los puntos resultantes.
Como se justificó en los fundamentos (Figura~\ref{fig:catmull_rom}), se ha elegido esta curva porque \textbf{atraviesa de forma exacta todos sus puntos de control}.

Esta propiedad es clave para que el movimiento sea seguro.
La fase anterior de línea de visión ya confirmó que los puntos de control están en espacio libre.
Obligar a la curva a pasar exactamente por ellos evita que el suavizado genere colisiones contra las paredes o el entorno.
La implementación para evaluar un punto en esta curva se muestra en la Figura~\ref{fig:catmull-eval}.

\begin{figure}[H]
\begin{lstlisting}[language={[Sharp]C}]
static Vector3 EvaluateCatmullRom(Vector3 p0, Vector3 p1, Vector3 p2, Vector3 p3, float t) =>
    0.5f * (
        2f * p1
        + (-p0 + p2) * t
        + (2f * p0 - 5f * p1 + 4f * p2 - p3) * (t * t)
        + (-p0 + 3f * p1 - 3f * p2 + p3) * (t * t * t)
    );
\end{lstlisting}
\caption{Evaluación de un segmento del \textit{spline} de Catmull-Rom.}
\label{fig:catmull-eval}
\end{figure}

\subsection{Validación}

La validación de esta iteración se centró en confirmar que el postprocesado no introduce colisiones.
Mediante pruebas que recorren la trayectoria final, se comprueba que tanto los segmentos del atajo como los puntos muestreados a lo largo de la \textit{spline} permanecen en espacio libre, contrastándolos con el motor de físicas.
La comprobación de línea de visión, por su carácter crítico, se verificó de forma aislada con casos en los que la presencia o ausencia de un obstáculo entre dos puntos es conocida de antemano, asegurando que el recorrido \acrshort{dda} no omite ningún \gls{voxel} intermedio.

\section{Iteración 5: Persistencia e integridad de la escena}
\label{sec:iter-persistencia}

\subsection{Análisis}

Reconstruir el \acrshort{svo} cada vez que se carga un escenario consumiría demasiado tiempo en ejecución.
Por ello, esta iteración aborda el proceso de \gls{bake}: la construcción anticipada del volumen desde el editor y su almacenamiento en disco para una carga inmediata posterior.
El análisis revela además un riesgo asociado: si la escena se modifica después del \gls{bake}, el volumen almacenado quedaría obsoleto y dejaría de ser fiable.

\subsection{Diseño}

Para resolver ambas cuestiones se toman dos decisiones de diseño.
El volumen se guarda como un \textbf{único bloque binario} (\textit{blob}), una representación plana y sin punteros que se carga de forma casi inmediata.
Para detectar escenas obsoletas, ese bloque se acompaña de un \textit{hash} de la geometría, calculado durante el \gls{bake} y comparado al cargar el nivel.

\subsection{Implementación}

\subsubsection{Serialización en un único bloque binario}

El \acrshort{svo} se serializa en un \textbf{único bloque binario} o \textit{blob} que se almacena dentro de un archivo de datos (\textit{asset}) del propio motor.
Cada hoja se guarda como un entero de 64 bits, mientras que cada nodo se representa mediante nueve enteros de 32 bits, los cuales almacenan su código de Morton, los enlaces a su primer hijo y a su padre, y sus seis vecinos.
Esta representación plana y sin punteros resulta muy compacta.
Además, acelera la carga en tiempo de ejecución, ya que el sistema se limita a leer de forma secuencial el flujo de bytes sin necesidad de reconstruir la jerarquía ni recalcular los vecinos.

\subsubsection{Integridad de la escena: reinterpretación de los bits de un \textit{float}}
\label{sec:hash-float}

Un problema habitual al almacenar estos datos es que el escenario cambie después de realizar el \gls{bake}, lo que dejaría el volumen obsoleto.
Para solucionar esto, el sistema calcula un \textit{hash} de la geometría relevante durante el \gls{bake} y lo compara con el de la escena actual al cargar el nivel.
Se emplea el algoritmo \gls{fnv1a} en lugar del método por defecto de C\#, ya que este último no garantiza el determinismo entre distintas ejecuciones del programa.

El algoritmo \gls{fnv1a} procesa los datos byte a byte.
Sin embargo, las posiciones, rotaciones y escalas de los objetos se definen con números en coma flotante (\texttt{float}).
Alimentar el algoritmo requiere obtener el patrón de bits \textbf{exacto} de cada \texttt{float} y no su valor numérico, el cual podría sufrir pérdidas por redondeo.
El inconveniente es que C\# no permite reinterpretar de forma directa los bits de un tipo de dato como si fuesen de otro.

La solución implementada consiste en crear una \textbf{unión}, una estructura común en lenguajes como C.
Como se muestra en la Figura~\ref{fig:float-union}, mediante el atributo \texttt{[StructLayout(LayoutKind.Explicit)]} se define una estructura donde un \texttt{float} y un \texttt{int} comparten la misma posición de memoria con \texttt{[FieldOffset(0)]}.
Al asignar el valor al campo \texttt{float}, es posible leer de forma inmediata el campo \texttt{int}, obteniendo la representación binaria exacta en forma de entero.
Esta técnica no realiza reservas de memoria adicionales, lo que garantiza el rendimiento y el determinismo necesarios para que el \textit{hash} sea fiable en cualquier sesión.

\begin{figure}[H]
\begin{lstlisting}[language={[Sharp]C}]
[StructLayout(LayoutKind.Explicit)]
struct FloatIntUnion
{
    [FieldOffset(0)] public float Float;
    [FieldOffset(0)] public int   Int;
}

// Devuelve el int cuya representacion de bits coincide con la del float.
public static int FloatBitsAsInt(float value) =>
    new FloatIntUnion { Float = value }.Int;
\end{lstlisting}
\caption{Reinterpretación de los bits de un \texttt{float} como un \texttt{int} mediante una unión.}
\label{fig:float-union}
\end{figure}

Finalmente, para evitar que el \textit{hash} dependa del orden aleatorio en el que el motor procesa los elementos, los componentes de colisión (\textit{colliders}) se ordenan de manera fija según sus transformaciones antes de ser procesados por el algoritmo.

\subsection{Validación}

Se comprobó, en primer lugar, que la serialización es reversible: un volumen construido en memoria, guardado y vuelto a cargar produce una estructura idéntica a la original, \gls{voxel} a \gls{voxel} y enlace a enlace.
En segundo lugar, se verificó el mecanismo de integridad.
Varias pruebas calculan el \textit{hash} de una escena, la modifican mínimamente (desplazando o reescalando un obstáculo) y confirman que el valor resultante cambia.
Cuando la geometría no varía, en cambio, el \textit{hash} permanece idéntico entre ejecuciones distintas, lo que valida el determinismo de \gls{fnv1a}.

\section{Iteración 6: Robustez geométrica y radio del agente}
\label{sec:iter-robustez}

\subsection{Análisis}

Las primeras versiones del buscador generaban, en ocasiones, trayectorias que rozaban o atravesaban la geometría del entorno.
Este problema, conocido como \textit{clipping}, ocurría sobre todo cerca de las aristas y esquinas de los obstáculos.
La causa de fondo era que el sistema trataba al agente como un simple punto sin volumen, por lo que ignoraba el espacio que este necesita para desplazarse.

\subsection{Diseño}

La solución adoptada consiste en \textbf{inflar} virtualmente los obstáculos utilizando el radio del agente durante el proceso de rasterización.
De esta forma, cualquier ruta que el sistema identifique como libre garantiza, por construcción, el margen necesario para que el agente se mueva sin colisionar.
La Figura~\ref{fig:clipping} ilustra este problema y su solución.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % ===== Izquierda: sin tratar el radio (colision) =====
    \begin{scope}
      \fill[obstaculo, rounded corners=1pt] (1.6,-1.2) rectangle (3.0,-2.6);
      \node[font=\footnotesize, text=white] at (2.3,-1.9) {obstáculo};
      \draw[flecha, draw=udcpink, very thick] (0,-0.2) -- (3.6,-2.05);
      \fill[ficblue!25, opacity=0.75] (1.85,-1.05) circle (0.34);
      \draw[agente] (1.85,-1.05) circle (0.34);
      \node[font=\scriptsize, text=udcpink!80!black, anchor=south] at (1.1,-0.32) {colisión};
      \draw[densely dotted, thin, draw=udcpink!80!black] (1.1,-0.42) -- (1.72,-1.0);
      \node[font=\footnotesize, anchor=north, text width=3.9cm, align=center] at (1.8,-3.05)
        {Sin tratar el radio: la trayectoria recorta la esquina};
    \end{scope}
    % ===== Derecha: obstaculo inflado (holgura) =====
    \begin{scope}[xshift=5.4cm]
      \draw[dashed, draw=udcpink, thick, rounded corners=4pt] (1.3,-0.9) rectangle (3.3,-2.9);
      \fill[obstaculo, rounded corners=1pt] (1.6,-1.2) rectangle (3.0,-2.6);
      \node[font=\footnotesize, text=white] at (2.3,-1.9) {obstáculo};
      \draw[flecha, draw=ficblue, very thick] (0,-0.2) -- (1.15,-0.5) -- (3.45,-0.5) -- (3.8,-2.0);
      \draw[<->, thin, draw=udcpink!80!black] (2.3,-0.5) -- (2.3,-0.9);
      \node[font=\scriptsize, text=udcpink!80!black, anchor=west] at (2.4,-0.7) {holgura $=$ radio};
      \node[font=\footnotesize, anchor=north, text width=4.0cm, align=center] at (1.9,-3.25)
        {Obstáculo inflado por el radio: trayectoria con holgura};
    \end{scope}
  \end{tikzpicture}
  \caption[Corrección del \textit{clipping} mediante el inflado de obstáculos por el radio del agente.]
  {Corrección del \textit{clipping} mediante el inflado de obstáculos por el radio del agente.
  Si no se considera el tamaño del agente, una trayectoria que recorta una esquina provoca colisión (izquierda).
  Al inflar el obstáculo por el radio del agente (margen discontinuo), la trayectoria conserva siempre la holgura necesaria (derecha).}
  \label{fig:clipping}
\end{figure}

\subsection{Implementación}

El inflado se integra en la canalización de construcción descrita en la Iteración~2 sin necesidad de una fase adicional: basta con expandir las cajas de consulta (\texttt{OverlapBox}) con el radio del agente.
Así, cualquier \gls{voxel} situado a una distancia de la geometría menor que el tamaño del agente queda marcado como ocupado de forma automática durante el muestreo.

\subsection{Validación}

Detectar de forma fiable estos errores geométricos, que eran intermitentes y dependían de cada escenario, exigió una batería de pruebas más amplia de lo previsto.
A las pruebas en modo de edición se añadieron pruebas en modo de ejecución (\textit{PlayMode}).
Estas instancian escenas completas, construyen el volumen y validan que las rutas generadas sean correctas utilizando el motor de físicas real de Unity.

Ambos tipos de pruebas se ejecutan automáticamente en el sistema de Integración Continua (\acrshort{ci}) con cada \textit{pull request}.
Así, cualquier fallo o regresión se detecta antes de integrar el código en la rama principal.

Este esfuerzo de validación sacó a la luz defectos sutiles que de otro modo habrían pasado inadvertidos.
Un ejemplo fue un desbordamiento aritmético al construir máscaras de bits.
La expresión \texttt{1 << 40} opera sobre un entero de 32 bits y desborda, así que hubo que forzar el literal a 64 bits con \texttt{1UL << 40}.
Este fallo se acotó con una prueba unitaria.
Otro defecto afectaba a la consistencia de los punteros del árbol.
La función de reserva de nodos hermanos (\textit{allocate missing siblings}) podía dejarlos en un estado incoherente.
Para detectarlo se añadió una prueba de integración que comprueba los invariantes de la estructura tras la construcción.
Aunque este esfuerzo extra de validación retrasó ligeramente la planificación, fue clave para asegurar la robustez del proyecto.

\section{Iteración 7: Integración en Unity y telemetría}
\label{sec:iter-integracion}

\subsection{Análisis}

Una vez consolidado el núcleo del sistema, esta iteración se centra en mejorar su usabilidad.
El objetivo es proporcionar herramientas accesibles tanto para perfiles técnicos como para diseñadores de niveles y artistas, que permitan configurar, depurar y dimensionar el volumen sin necesidad de leer el código.

\subsection{Diseño}

\subsubsection{Componentes y modos de construcción}

El elemento central es el componente \texttt{NavVolumeSpace}, que gestiona el volumen de navegación.
Para adaptarse a diferentes flujos de trabajo, ofrece tres modos de construcción: \textbf{precalculado} (\textit{Baked}), que carga los datos guardados previamente en disco, \textbf{construido al inicio} (\textit{BuildOnAwake}) y \textbf{manual}, que se construye al invocar la función correspondiente desde código.
Junto a este, se incluyen componentes adicionales para representar a las entidades del escenario, como los agentes y los obstáculos dinámicos.

\subsection{Implementación}

\subsubsection{Visualización mediante Gizmos}

Para facilitar la depuración, se ha implementado una representación visual del \acrshort{svo} en la vista de escena mediante Gizmos.
Esta herramienta muestra la estructura del árbol utilizando un código de colores para identificar el estado de cada elemento: nodos vacíos, nodos con geometría, hojas parciales y \glspl{voxel} ocupados (véase la Figura~\ref{fig:gizmos}).
Como un volumen puede contener cientos de miles de nodos, el sistema de dibujado incluye dos medidas para mantener el rendimiento: un \textbf{límite máximo} de elementos a renderizar y un \textbf{descarte por distancia} respecto a la cámara, evitando dibujar detalles innecesarios en zonas alejadas.

\begin{figure}[htp]
  \centering
  \includegraphics[width=0.75\textwidth]{imaxes/svogizmos.png}
  \caption[Gizmos dibujados por el volumen.]
  {Gizmos dibujados por el volumen.
  En rojo cada uno de los vóxeles, en amarillo los nodos hoja y en verde el resto de nodos.
  Imagen obtenida por medio de una captura de pantalla.}
  \label{fig:gizmos}
\end{figure}

\subsubsection{Telemetría: el cálculo de la memoria estimada}
\label{sec:memoria-estimada}

El inspector de Unity incluye ahora un panel de estadísticas para ayudar al usuario a configurar el volumen correctamente (Figura~\ref{fig:stats-panel}).
Las dos métricas principales son el porcentaje de \glspl{voxel} ahorrados frente a un modelo tradicional y una \textbf{estimación} de la memoria que ocupa la estructura.

Este cálculo de memoria se basa en cómo C\# gestiona los objetos.
En sistemas de 64 bits, cualquier objeto en memoria dinámica (\gls{heap}) incluye datos extra de control (\textit{overhead}), como una cabecera y, en el caso de los vectores (\textit{arrays}), el espacio para almacenar su longitud.
Utilizando estas constantes aproximadas, el sistema recorre la jerarquía sumando la cabecera de cada vector al tamaño real de sus elementos (calculado mediante \texttt{Unsafe.SizeOf}), tal y como se detalla en la Figura~\ref{fig:memoria-pseudo}.

\begin{figure}[H]
\begin{lstlisting}[language={[Sharp]C}]
const int _OBJECT_HEADER_BYTES = 24; // cabecera de objeto
const int _ARRAY_HEADER_BYTES  = 24; // cabecera de objeto + longitud
const int _REFERENCE_BYTES     = 8;  // tamano de una referencia

int total = _OBJECT_HEADER_BYTES;

// Vector de hojas: cada hoja es un ulong (8 bytes).
total += _ARRAY_HEADER_BYTES + Unsafe.SizeOf<SVOLeaf>() * leaves.Length;

// Vector externo de capas (un vector dentado de referencias).
total += _ARRAY_HEADER_BYTES + _REFERENCE_BYTES * layers.Length;

// Cada capa: su cabecera mas el tamano real de cada nodo.
foreach (var layer in layers)
    total += _ARRAY_HEADER_BYTES + Unsafe.SizeOf<SVONode>() * layer.Length;
\end{lstlisting}
\caption{Estimación de la memoria ocupada por el \acrshort{svo}.}
\label{fig:memoria-pseudo}
\end{figure}

Conviene recordar que este valor es una \textbf{estimación} orientativa, pensada para comparar rápidamente distintas configuraciones.
Por otro lado, el ahorro de espacio se calcula comparando los \glspl{voxel} que realmente se han creado en memoria frente a los que tendría un árbol completamente denso de la misma profundidad (el cual requiere $8^{(L-1)}$ hojas para $L$ capas).
Esta cifra muestra con claridad la principal ventaja de una representación jerárquica dispersa.

\begin{figure}[htp]
  \centering
  \includegraphics[width=0.75\textwidth]{imaxes/bakestats.png}
  \caption[Estadísticas del volumen construido.]
  {Estadísticas del volumen construido.
  Imagen obtenida por medio de una captura de pantalla.}
  \label{fig:stats-panel}
\end{figure}

\subsection{Validación}

Por su carácter de herramientas de apoyo, estos componentes se validaron de forma mayoritariamente cualitativa y manual dentro del editor.
La visualización mediante Gizmos cumple además una doble función, pues fue la principal herramienta para inspeccionar visualmente que el volumen construido se ajusta a la geometría de cada escena de prueba.
El cálculo de la memoria estimada se contrastó comparando su resultado con el tamaño real de las estructuras en escenarios sencillos, confirmando que la aproximación se mantiene dentro de un margen razonable y resulta útil para comparar configuraciones.

\section{Iteración 8: API pública, optimización del rendimiento}
\label{sec:iter-api-opt}

\subsection{Análisis}

En esta iteración se consolida la interfaz pública del sistema y se abordan las tareas de evaluación y optimización del rendimiento.
Se busca que la \acrshort{api} cubra los casos de uso más habituales y que el sistema rinda adecuadamente incluso en escenarios exigentes con numerosas peticiones de ruta.

\subsection{Diseño}

\subsubsection{API de navegación y consultas espaciales}

El componente \texttt{NavVolumeSpace} expone una \acrshort{api} diseñada para cubrir los casos de uso más habituales.
Además del cálculo de rutas, incluye consultas espaciales útiles para poblar escenas o dirigir el movimiento de los agentes.
Destacan funciones como comprobar si un punto es navegable, ajustarlo al \gls{voxel} libre más cercano (\textit{snapping}) o generar posiciones aleatorias válidas.

Para el cálculo de trayectorias, el sistema ofrece dos variantes:

\begin{itemize}
  \item \textbf{Síncrona:} Reutiliza una única instancia del buscador y opera de manera secuencial en el hilo principal.
  \item \textbf{Asíncrona:} Ejecuta la búsqueda en un hilo secundario para evitar bloquear el juego en escenarios con muchas peticiones.
  Utiliza una reserva de buscadores (\textit{pool}) para ahorrar memoria y permite cancelar la operación en curso mediante un \textit{token}.
\end{itemize}

La Figura~\ref{fig:seq-path} detalla la secuencia de una petición asíncrona, desde la llamada del agente hasta la entrega de la trayectoria suavizada.

\begin{figure}[htp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      head/.style={draw=ficblue!70!black, fill=ficblue!12, thick, rounded corners=2pt,
                   align=center, font=\footnotesize, minimum height=8mm, inner xsep=5pt},
      life/.style={dashed, draw=black!45},
      msg/.style={-{Stealth[length=2.4mm]}, thick},
      ret/.style={-{Stealth[length=2.4mm]}, dashed, draw=black!60},
      lbl/.style={font=\scriptsize, inner sep=1.5pt, fill=white},
      act/.style={fill=ficblue!22, draw=ficblue!70!black, line width=0.3pt}
    ]

    % Coordenadas X de las lineas de vida
    \def\xA{0}     % Agente
    \def\xN{3.6}   % NavVolumeSpace
    \def\xP{6.7}   % Pool
    \def\xS{9.1}   % SVOPathfinder
    \def\xM{11.9}  % PathSmoother
    \def\ybot{-9.6}

    % Cabeceras
    \node[head] (hA) at (\xA,0) {Agente};
    \node[head] (hN) at (\xN,0) {NavVolumeSpace};
    \node[head] (hP) at (\xP,0) {Pool de\\buscadores};
    \node[head] (hS) at (\xS,0) {SVOPathfinder\\(A*)};
    \node[head] (hM) at (\xM,0) {PathSmoother};

    % Lineas de vida
    \foreach \x in {\xA,\xN,\xP,\xS,\xM}
      \draw[life] (\x,-0.45) -- (\x,\ybot);

    % Marco del trabajo en hilo secundario (capa normal, antes de barras y mensajes)
    \draw[draw=udcgray!70, thick, rounded corners=2pt] (\xN-1.05,-2.2) rectangle (\xM+1.05,-8.5);

    % Barras de activacion
    \filldraw[act] (\xN-0.09,-1.0)  rectangle (\xN+0.09,-9.1);
    \filldraw[act] (\xP-0.09,-2.8)  rectangle (\xP+0.09,-3.3);
    \filldraw[act] (\xS-0.09,-3.9)  rectangle (\xS+0.09,-5.3);
    \filldraw[act] (\xM-0.09,-5.9)  rectangle (\xM+0.09,-6.4);
    \filldraw[act] (\xM-0.09,-7.0)  rectangle (\xM+0.09,-7.5);
    \filldraw[act] (\xP-0.09,-8.1)  rectangle (\xP+0.09,-8.3);

    % Mensajes
    \draw[msg] (\xA,-1.0) -- (\xN,-1.0) node[lbl, midway, above] {FindPathAsync(request, token)};
    \draw[ret] (\xN,-1.7) -- (\xA,-1.7) node[lbl, midway, above] {Task\textless PathResult\textgreater};

    \draw[msg] (\xN,-2.8) -- (\xP,-2.8) node[lbl, midway, above] {TryTake()};
    \draw[ret] (\xP,-3.3) -- (\xN,-3.3) node[lbl, midway, above] {buscador (o nuevo)};

    \draw[msg] (\xN,-3.9) -- (\xS,-3.9) node[lbl, midway, above] {FindPath(ctx, request)};
    % Bucle interno de A*
    \draw[msg] (\xS+0.09,-4.5) -- ++(0.7,0) -- ++(0,-0.4) -- (\xS+0.09,-4.9);
    \node[lbl, anchor=west] at (\xS+0.85,-4.7) {bucle A*};
    \draw[ret] (\xS,-5.3) -- (\xN,-5.3) node[lbl, midway, above] {ruta en bruto};

    \draw[msg] (\xN,-5.9) -- (\xM,-5.9) node[lbl, midway, above] {GreedyShortcut(raw)};
    \draw[ret] (\xM,-6.4) -- (\xN,-6.4) node[lbl, midway, above] {atajo (LOS / DDA)};

    \draw[msg] (\xN,-7.0) -- (\xM,-7.0) node[lbl, midway, above] {CatmullRomSpline(atajo)};
    \draw[ret] (\xM,-7.5) -- (\xN,-7.5) node[lbl, midway, above] {ruta suavizada};

    \draw[msg] (\xN,-8.1) -- (\xP,-8.1) node[lbl, midway, above] {Add(buscador)};

    \draw[ret] (\xN,-9.1) -- (\xA,-9.1) node[lbl, midway, above] {PathResult};

    % Etiqueta del fragmento, dibujada al final (opaca) para no quedar bajo la barra de NavVolumeSpace
    \node[anchor=north west, fill=white, draw=udcgray!70, rounded corners=1pt,
          inner xsep=4pt, inner ysep=2pt, font=\scriptsize\itshape, text=udcgray!85!black]
      at (\xN-1.05,-2.2) {hilo secundario (\texttt{Task.Run})};

  \end{tikzpicture}%
  }
  \caption[Secuencia de una petición de ruta asíncrona.]
  {Secuencia de una petición de ruta asíncrona.
  Tras devolver de inmediato una \texttt{Task}, el cálculo prosigue en un hilo secundario: se toma un buscador de la reserva, se ejecuta la búsqueda \gls{astar}, se postprocesa la ruta (atajo por línea de visión y suavizado por \textit{spline}) y se devuelve el buscador a la reserva antes de entregar el resultado.
  La variante síncrona reutiliza un único buscador y prescinde del hilo secundario.}
  \label{fig:seq-path}
\end{figure}

\subsection{Implementación}

\subsubsection{Optimizaciones de rendimiento}

La fase de optimización se guió por el perfilado del rendimiento e introdujo mejoras de bajo nivel basadas en los principios del \acrshort{dod} (\textit{Data-Oriented Design}).
Las más destacadas son:

\begin{itemize}
  \item \textbf{Reutilización de \textit{buffers}:} El buscador recicla las colecciones de su estado interno entre peticiones, reduciendo la carga sobre el recolector de basura (\textit{Garbage Collector}).
  \item \textbf{Comprobación de cancelación amortizada:} Para evitar el alto coste de consultar el \textit{token} en cada paso de la búsqueda, la comprobación se realiza solo de forma periódica utilizando una máscara de bits.
  \item \textbf{Acceso por referencia de solo lectura:} La consulta de nodos devuelve referencias (\texttt{ref readonly}) en lugar de copias completas de la estructura, ahorrando tiempo y memoria en las operaciones más frecuentes.
  \item \textbf{Comparación sin \textit{boxing}:} Tal y como se mencionó en la Sección~\ref{sec:svolink}, los enlaces implementan \texttt{IEquatable} para funcionar de manera eficiente como claves en las colecciones \textit{hash} del buscador.
\end{itemize}

Por último, se optimizó la construcción del propio volumen de navegación.
Al evitar reordenar las capas cuando no faltan nodos hermanos, se logró reducir significativamente el tiempo de procesado (\gls{bake}) en escenarios densos.

\subsection{Validación}

La validación de esta iteración tuvo una doble vertiente.
Por un lado, la corrección funcional de la \acrshort{api} y de las consultas espaciales se aseguró con la batería de pruebas existente, que se ejecuta de forma automática en el sistema de \acrshort{ci} y evita que las optimizaciones introduzcan regresiones.
Por otro, las mejoras de rendimiento se guiaron por el perfilado (\textit{profiling}).
Cada optimización se midió comparando el tiempo de ejecución y las reservas de memoria antes y después del cambio.
Así se confirmó de forma objetiva la menor carga sobre el recolector de basura y la aceleración del \gls{bake}.

\section{Iteración 9: Evasión local entre agentes}
\label{sec:iter-evasion}

\subsection{Análisis}

Esta última iteración introduce una funcionalidad extra (véase la Sección~\ref{sec:seguimiento}): la evasión local entre agentes.
Hasta ahora, el cálculo de rutas evitaba la geometría estática, pero no impedía que los agentes colisionaran entre sí al moverse por el escenario.
Resolver esa carencia es el objetivo de esta fase.

\subsection{Diseño}

\subsubsection{El algoritmo ORCA}

Para solucionar este problema se implementó el algoritmo \acrshort{orca}~\cite{van2011reciprocal}.
Tal y como se explicó en los fundamentos, este método analiza el espacio de velocidades para calcular, en cada instante, una velocidad segura que sea lo más parecida posible a la trayectoria deseada.
La implementación se basó en adaptar las rutinas tridimensionales de la biblioteca original RVO2-3D~\cite{rvo2_3d}.

El algoritmo busca la velocidad óptima respetando diversas restricciones, representadas como semiplanos.
Si el problema resulta infactible por un exceso de restricciones simultáneas, aplica una relajación matemática para minimizar el impacto de la colisión, tal y como ilustra la Figura~\ref{fig:orca}.

\subsection{Implementación}

Dado que este cálculo se ejecuta para cada agente en cada fotograma, su optimización es crítica.
Para evitar problemas de rendimiento en esta área del sistema:

\begin{itemize}
  \item Se utiliza \texttt{stackalloc} para reservar las estructuras temporales directamente en el \gls{stack}.
  \item Se manipulan los datos mediante \texttt{Span\textless T\textgreater}.
\end{itemize}

Ambas técnicas evitan asignar memoria en el \gls{heap}, eliminando por completo la carga de trabajo sobre el recolector de basura.

Además, el sistema no solo evita colisiones entre agentes, sino que también detecta los \glspl{voxel} bloqueados en el \acrshort{svo} cercano.
De este modo, el comportamiento de evasión local también respeta la geometría estática del entorno de forma fluida.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[font=\small]
    % Region de velocidades prohibidas (semiplano sombreado)
    \fill[udcpink!16] (-2.6,0.1) -- (2.4,2.6) -- (2.8,2.8) -- (-2.6,2.8) -- cycle;
    % Ejes del espacio de velocidades
    \draw[->, thick] (-2.7,0) -- (2.9,0) node[right, font=\footnotesize] {$v_x$};
    \draw[->, thick] (0,-2.4) -- (0,2.9) node[above, font=\footnotesize] {$v_y$};
    \node[font=\scriptsize, anchor=north east] at (-0.05,-0.05) {$0$};
    % Recta de restriccion ORCA
    \draw[udcpink, thick] (-2.6,0.1) -- (2.4,2.6);
    \node[font=\scriptsize, text=udcpink, rotate=26] at (-1.35,0.95) {restricción ORCA};
    % Etiquetas de regiones
    \node[font=\scriptsize, text=udcpink!85!black] at (-1.3,2.1) {velocidades prohibidas};
    \node[font=\scriptsize, text=black!65] at (1.5,-1.2) {velocidades permitidas};
    % Velocidad preferida (cae en zona prohibida)
    \draw[->, very thick, draw=ficblue] (0,0) -- (0.9,2.2) node[above, font=\scriptsize, text=ficblue] {$v^{\mathrm{pref}}$};
    % Velocidad optima: proyeccion ortogonal de v_pref sobre la recta de restriccion
    \draw[dashed, thin, draw=black!55] (0.9,2.2) -- (1.04,1.92);
    \draw[->, very thick, draw=black] (0,0) -- (1.04,1.92);
    \fill[black] (1.04,1.92) circle (1.3pt);
    \node[font=\scriptsize, anchor=west] at (1.12,1.72) {$v^{\mathrm{new}}$};
  \end{tikzpicture}
  \caption[Resolución de la velocidad segura en el espacio de velocidades de ORCA.]
  {Resolución de la velocidad segura en el espacio de velocidades de ORCA.
  Cada agente vecino y la geometría inducen un semiplano de velocidades prohibidas (zona sombreada).
  Como la velocidad preferida $v^{\mathrm{pref}}$ cae en una región prohibida, el algoritmo escoge la velocidad factible $v^{\mathrm{new}}$ más próxima, sobre la frontera de la región segura.}
  \label{fig:orca}
\end{figure}

\subsubsection{Integración con el sistema de \textit{jobs}}

Dado que cada agente resuelve su propia evasión de forma independiente, el proceso es altamente paralelizable.
Por ello, se implementó utilizando el \textit{Job System} y el compilador Burst, aplicando las siguientes estrategias:

\begin{itemize}
  \item \textbf{\textit{Hash} espacial:} Permite localizar rápidamente a los agentes vecinos cercanos sin tener que compararlos con todos los agentes de la escena, ahorrando cálculos innecesarios.
  \item \textbf{Ejecución asíncrona:} Los \textit{jobs} se programan al final de un fotograma y se recogen al inicio del siguiente. Esto permite que la simulación se calcule en paralelo al renderizado del juego, reduciendo al mínimo su impacto en la tasa de fotogramas.
\end{itemize}

\subsection{Validación}

La evasión local se validó en escenas con múltiples agentes que comparten destino o cruzan trayectorias opuestas, situaciones que sin evasión derivarían en colisiones.
Mediante pruebas en modo de ejecución (\textit{PlayMode}) se comprobó que las velocidades resultantes mantienen a los agentes separados entre sí y respetan la geometría estática, y que el sistema permanece estable, sin bloqueos mutuos persistentes, a lo largo de la simulación.
La inspección visual en el editor complementó estas pruebas, permitiendo observar en tiempo real el comportamiento emergente del grupo de agentes.

\section{Discusión global del sistema}
\label{sec:discusion-global}

El sistema final conforma un \textit{pipeline} con dos etapas bien diferenciadas, ilustradas en la Figura~\ref{fig:flujo-datos}.

\begin{enumerate}
  \item \textbf{Preprocesado en el editor (\gls{bake}):} Transforma la geometría de la escena en un \acrshort{svo} y lo guarda en disco.
  Esta fase asume a propósito el mayor coste computacional (consultas físicas, construcción de la jerarquía y cálculo de vecindad), ya que se ejecuta una sola vez y fuera del tiempo real.
  \item \textbf{Ejecución en tiempo real:} Carga el volumen precalculado de forma inmediata para resolver peticiones de ruta (búsqueda, atajo y suavizado) y simular la evasión local.
  Su diseño prioriza el rendimiento con estructuras de datos compactas, paralelización con el \textit{Job System} y Burst, y una gestión de la memoria que minimiza el impacto del recolector de basura.
\end{enumerate}

\begin{figure}[htp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[font=\small, node distance=7mm and 9mm,
      contA/.style={rounded corners=5pt, draw=ficblue!70, very thick, fill=none, inner sep=8pt},
      contB/.style={rounded corners=5pt, draw=udcpink!70, very thick, fill=none, inner sep=8pt}]
    % --- Fase de preprocesado (editor): banda superior ---
    \node[paso] (escena) {Escena};
    \node[paso, right=of escena] (raster) {Rasterización};
    \node[paso, right=of raster] (svo) {\acrshort{svo}};
    \node[almacen, right=of svo] (disco) {Volumen\\en disco};
    % --- Fase de ejecucion (tiempo real): banda inferior ---
    \node[paso, below=16mm of escena] (carga) {Carga del\\volumen};
    \node[paso, right=of carga] (astar) {Búsqueda A*};
    \node[paso, right=of astar] (postp) {Post-\\procesado};
    \node[paso, right=of postp] (evasion) {Evasión\\local};
    \node[paso, right=of evasion] (movim) {Movimiento\\del agente};
    % --- Conexiones dentro de cada banda ---
    \draw[flecha] (escena) -- (raster);
    \draw[flecha] (raster) -- (svo);
    \draw[flecha] (svo) -- (disco);
    \draw[flecha] (carga) -- (astar);
    \draw[flecha] (astar) -- (postp);
    \draw[flecha] (postp) -- (evasion);
    \draw[flecha] (evasion) -- (movim);
    % --- Traspaso entre fases: el volumen serializado se vuelve a cargar ---
    \draw[flecha, rounded corners=3pt] (disco.south) -- ++(0,-5mm) -| (carga.north);
    % --- Contenedores de fase (solo borde, en capa normal) ---
    \node[contA, fit=(escena)(raster)(svo)(disco)] (cajaA) {};
    \node[contB, fit=(carga)(astar)(postp)(evasion)(movim)] (cajaB) {};
    \node[font=\footnotesize\itshape, text=ficblue, anchor=south west]
      at ([yshift=1.5mm]cajaA.north west) {Preprocesado (editor)};
    \node[font=\footnotesize\itshape, text=udcpink!85!black, anchor=north west]
      at ([yshift=-1.5mm]cajaB.south west) {Ejecución (tiempo real)};
  \end{tikzpicture}%
  }
  \caption[Flujo de datos global del sistema, del preprocesado a la ejecución.]
  {Flujo de datos global del sistema, del preprocesado a la ejecución.
  En el editor, la escena se rasteriza para construir el \acrshort{svo}, que se serializa en disco.
  En tiempo de ejecución, el volumen se carga y, sobre él, se resuelven las consultas de ruta (búsqueda, postprocesado y evasión local) que culminan en el movimiento del agente.}
  \label{fig:flujo-datos}
\end{figure}

A lo largo del proyecto, varias decisiones clave de diseño han resultado fundamentales para el éxito del sistema:

\begin{itemize}
  \item \textbf{Representación compacta:} El uso de máscaras de bits para las hojas y enlaces de 32 bits reduce mucho el consumo de memoria y los fallos de caché.
  \item \textbf{Estrategia de lo grueso a lo fino:} Aplicada tanto al construir el volumen como al buscar rutas, evita procesar información a máxima resolución cuando no es estrictamente necesario.
  \item \textbf{Arquitectura modular:} La separación del código en bloques independientes permitió validar cada nueva funcionalidad por separado y mantener la estabilidad del sistema en todo momento.
\end{itemize}

Como se detalla en la Figura~\ref{fig:arquitectura-final}, esta arquitectura modular creció de forma controlada. El módulo \textit{Runtime} final se organiza en cinco espacios de nombres que agrupan los componentes principales y sus dependencias sobre una base tecnológica común llamada \texttt{Core}.

\begin{figure}[htp]
  \centering
  \begin{tikzpicture}[
      font=\small,
      nsbox/.style={rounded corners=3pt, draw=udcpink!60!black, fill=white, thick,
                    align=center, text width=3.3cm, inner sep=5pt, minimum height=1.3cm,
                    font=\footnotesize},
      ancho/.style={text width=12.2cm},
      usedep/.style={-{Stealth[length=2.6mm]}, semithick, draw=ficblue!75!black}
    ]

    % --- Capa intermedia: construccion, busqueda y evasion ---
    \node[nsbox] (path) {\textbf{Pathfinding}\\[1pt]\scriptsize SVOPathfinder, PathSmoother,\\ SVORaycast (DDA), \textit{Api}};
    \node[nsbox, left=9mm of path] (builder)
      {\textbf{Builder}\\[1pt]\scriptsize SVOBuilder, SVORasterizer,\\ SVONeighborLinker,\\ \textit{Jobs}, \textit{Reporting}};
    \node[nsbox, right=9mm of path] (avoid)
      {\textbf{Avoidance}\\[1pt]\scriptsize AvoidanceSimulation,\\ \textit{Orca}, \textit{Spatial}};

    % --- Cimiento (Core) y capa superior (Unity), a lo ancho ---
    \node[nsbox, ancho, below=10mm of path] (core)
      {\textbf{Core}\\[1pt]\scriptsize MortonCode, SVO, SVONode, SVOLeaf, SVOLink, NeighborSet};
    \node[nsbox, ancho, above=10mm of path] (unity)
      {\textbf{Unity}\\[1pt]\scriptsize NavVolumeSpace, NavVolumeAgent, NavVolumeObstacle, \textit{Hashing}};

    % --- Dependencias (A -> B: A usa B) ---
    \draw[usedep] (builder.south) -- (builder.south |- core.north);
    \draw[usedep] (path.south)    -- (path.south    |- core.north);
    \draw[usedep] (avoid.south)   -- (avoid.south   |- core.north);
    \draw[usedep] (path.west)     -- (builder.east);
    \draw[usedep] (unity.south -| builder.north) -- (builder.north);
    \draw[usedep] (unity.south -| path.north)    -- (path.north);
    \draw[usedep] (unity.south -| avoid.north)   -- (avoid.north);

    % --- Contenedor Runtime ---
    \begin{scope}[on background layer]
      \node[contenedor, fit=(core)(builder)(path)(avoid)(unity),
            label={[font=\bfseries]above:Runtime}] (runtime) {};
    \end{scope}

    % --- Modulos Editor y Tests (dependen solo de Runtime) ---
    \node[modulo, fill=ficblue!15, draw=ficblue,
          above=14mm of runtime.north west, anchor=south west]
      (editor) {\textbf{Editor}\\[2pt]\footnotesize Herramientas de\\configuración y\\visualización};
    \node[modulo, fill=udcgray!40, draw=udcgray!80!black,
          above=14mm of runtime.north east, anchor=south east]
      (tests) {\textbf{Tests}\\[2pt]\footnotesize Pruebas \textit{EditMode}\\y \textit{PlayMode}};
    \draw[dep] (editor.south) -- (editor.south |- runtime.north);
    \draw[dep] (tests.south)  -- (tests.south  |- runtime.north);

  \end{tikzpicture}
  \caption[Arquitectura final del módulo \textit{Runtime} y sus dependencias internas.]
  {Arquitectura final del módulo \textit{Runtime} y sus dependencias internas.
  Sobre el cimiento común \texttt{Core} se asientan \texttt{Builder}, \texttt{Pathfinding} y \texttt{Avoidance} (este último incorporado en la última iteración), y \texttt{Unity} orquesta a todos ellos exponiendo el sistema al motor.
  Las flechas señalan las dependencias principales entre espacios de nombres. Los módulos \textit{Editor} y \textit{Tests} siguen dependiendo únicamente de \textit{Runtime} (cf.\ Figura~\ref{fig:arquitectura}).}
  \label{fig:arquitectura-final}
\end{figure}

TODO añadir métricas temporales.
